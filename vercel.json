{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @issue-tracker/web-frontend build",
  "outputDirectory": "apps/web-frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
