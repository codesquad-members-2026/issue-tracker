{
  "name": "issue-tracker",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "api:spec": "cd apps/spring-api && ./gradlew generateOpenApiDocs && cd ../.. && pnpm --filter @issue-tracker/api-spec generate"
  }
}
